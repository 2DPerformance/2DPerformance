function $(t={}){const e=Math.max(0,Math.round(Number(t.count)||0)),n=Math.max(6,Number(t.diaMm)||12),c=Math.max(100,Number(t.beamWidthMm)||200),M=Math.max(25,Number(t.coverMm)||40),o=Math.max(6,Number(t.stirrupDiaMm)||9),a=Math.max(10,Number(t.aggregateMm)||19),r=Math.max(25,n,4/3*a),s=Math.max(0,c-2*(M+o)),u=Math.floor((s+r)/(n+r)),i=Math.max(2,u),h=e>0?Math.max(1,Math.ceil(e/i)):0,y=h>0?Math.floor(e/h):0,f=h>0?e%h:0,m=Array.from({length:h},(g,N)=>y+(N<f?1:0)).filter(g=>g>0),x=[];return e>0&&u<2&&x.push("หน้ากว้างคานแคบเกินสำหรับจัดเหล็ก 2 เส้นต่อชั้น"),m.length>2&&x.push(`ต้องจัดเหล็ก ${m.length} ชั้น ควรตรวจระยะเทคอน/เพิ่มหน้าตัด`),{count:e,diaMm:n,maxPerLayer:i,rawMaxPerLayer:u,layers:m,layerCount:m.length,perLayer:Math.max(...m,0),minClearMm:r,clearWidthMm:s,layerText:m.length>1?`จัด ${m.length} ชั้น (${m.join("+")})`:"จัด 1 ชั้น",warnings:x}}const l={ok:0,warn:1,fail:2};function b(t,e,n,c,M,o){const a=$({count:n,diaMm:e,beamWidthMm:t,stirrupDiaMm:c,coverMm:M,aggregateMm:o});let r="ok",s=a.layerText+" · พอดี";return n>0&&a.rawMaxPerLayer<2?(r="fail",s=`ใส่ไม่ลง — กว้างไม่พอวาง 2 เส้น/ชั้น (DB${e} · กว้างสุทธิ ${Math.round(a.clearWidthMm)}mm) → ต้องเพิ่มหน้าตัด`):e>=28?(r="warn",s=`แน่น — DB${e} เส้นใหญ่ (${a.layerText}) ควรพิจารณาเพิ่มหน้าตัด/ใช้เส้นเล็กลง`):t<=200&&e>=25?(r="warn",s=`ค่อนข้างแน่น — DB${e} ในคานกว้าง ${t/10}cm (${a.layerText})`):a.layers.length>2&&(r="warn",s=`ต้องจัด ${a.layers.length} ชั้น — ตรวจระยะหุ้ม/เทคอนกรีต`),{status:r,layout:a,note:s}}function w(t){const e=Math.max(6,Number(t.stirrupDiaMm)||9),n=Math.max(25,Number(t.coverMm)||40),c=Math.max(10,Number(t.aggregateMm)||19),M=Math.max(100,Number(t.beamWidthMm)||200),o=b(M,Math.max(6,t.topDiaMm||12),Math.max(0,t.topN||0),e,n,c),a=b(M,Math.max(6,t.botDiaMm||12),Math.max(0,t.botN||0),e,n,c),r=l[o.status]>=l[a.status]?o.status:a.status,s=l[o.status]>=l[a.status]?o:a;return{status:r,top:o,bot:a,clearWidthMm:a.layout.clearWidthMm,note:s.note,reasons:[`บน: ${o.note}`,`ล่าง: ${a.note}`]}}export{w as b,$ as g};
